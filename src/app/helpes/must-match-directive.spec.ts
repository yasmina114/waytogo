import { MustMatchDirective } from './must-match-directive';

describe('MustMatchDirective', () => {
  it('should create an instance', () => {
    expect(new MustMatchDirective()).toBeTruthy();
  });
});
